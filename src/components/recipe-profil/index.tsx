import { StyledRecipeProfile } from "./styles";

interface RecipeProfileProps {}

const RecipeProfile = (props: RecipeProfileProps) => {
  return <StyledRecipeProfile></StyledRecipeProfile>;
};

export default RecipeProfile;
